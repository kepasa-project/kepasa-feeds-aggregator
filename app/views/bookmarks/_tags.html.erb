<div class="row">
	<div class="span12">
			<div id="tag_cloud">
  			<% tag_cloud @bookmarks.tag_counts, %w{s m l} do |tag, css_class| %>
    		<%= link_to tag.name, tag_path(tag.name), class: css_class %> <br />
  			<% end %>
	        </div>
    </div>
</div>