<%= form_for(@newspaper) do |f| %>
  <% if @newspaper.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@newspaper.errors.count, "error") %> prohibited this newspaper from being saved:</h2>

      <ul>
      <% @newspaper.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Periodico %><br />
    <%= f.collection_select :rssurl, Newspaper.order(:title), :id, :title, {:prompt => true} %>
  </div>
  <div class="actions">
    <%= f.submit 'Insert Noticias', :class => 'btn-primary' %>
  </div>
<% end %>